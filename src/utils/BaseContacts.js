var baseContacts = function( contact ){
	var fixtureA = contact.GetFixtureA();
	var fixtureB = contact.GetFixtureB();
	if ( fixtureA.userData == undefined )
	{
		return;
	}
	switch( fixtureA.userData.type )
	{
		
	}
}
